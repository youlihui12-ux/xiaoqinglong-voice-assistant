# Contributing

Keep this project local-first and privacy-preserving.

Before opening a pull request:

1. Run `npm test`.
2. Run a secret scan with `rg` or your preferred scanner.
3. Do not include personal logs, task queues, screenshots, Agent IDs, API keys, or absolute user paths.
4. Document new environment variables in `.env.example` and `README.md`.
